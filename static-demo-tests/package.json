{
  "name": "clever-static-demo-tests",
  "private": true,
  "version": "1.0.0",
  "description": "On-demand Playwright suite that exercises every feature of the static Lead Slip Detector demo (clever-case-study-backend). Self-contained; not served by GitHub Pages.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
